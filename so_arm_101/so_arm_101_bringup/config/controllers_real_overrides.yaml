# Only values that differ from the common controller configuration.
controller_manager:
  ros__parameters:
    use_sim_time: false
    update_rate: 30

arm_controller:
  ros__parameters:
    state_publish_rate: 30.0

gripper_controller:
  ros__parameters:
    state_publish_rate: 30.0
