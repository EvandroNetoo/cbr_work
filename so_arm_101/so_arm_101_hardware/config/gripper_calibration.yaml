# Conversion between the adapted gripper's ROS prismatic joint and its servo.
# These endpoints are existing project values and must be confirmed on the
# physical mechanism before changing them.
gripper:
  motor_closed_position_rad: 0.0
  motor_open_position_rad: 1.7428
